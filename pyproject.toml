[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "emergence-simulator"
version = "0.1.0"
requires-python = ">=3.9"

[project.scripts]
emergence-simulator = "emergence_simulator.__main__:main"

[tool.pytest.ini_options]
pythonpath = ["."]
